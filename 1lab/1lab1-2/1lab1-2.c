#include <stdio.h> //Библиотека
int main () // Определение функции main без аргументов
{
 int a, b = 5, c; // Объявление трех целочисленных переменных: a, b и c, присвоение переменной b значения 5
 double x, y = -.5, z=4; // Объявление трех вещественных переменных: x, y и z, присвоение переменным y и z значений -0.5 и 4 соответственно
 x = c = a = 2; // Присваивание значения 2 переменным a, c и x
 printf("x = c = a : a=%d c=%d x=%f\n", a, c, x); // Вывод значения переменных a, c и x на экран
 a += b; // Увеличение значения переменной a на значение переменной b
 printf("a += b : a=%d\n", a); // Вывод значения переменной a на экран
 x *= b + a; // Умножение значения переменной x на сумму переменных b и a
 printf("x *= b+a : x=%lf\n", x); // Вывод значения переменной x на экран
 b += a--; // Увеличение значения переменной b на значение переменной a, затем уменьшение значения переменной a на 1
 printf("b += a-- : a=%d b=%d\n", a, b); // Вывод значений переменных a и b на экран
 x -= ++c; // Уменьшение значения переменной x на значение переменной c, увеличенное на 1
 printf("x -= ++c : c=%d x=%lf\n\n", c, x); // Вывод значений переменных c и x на экран

a = 13; b = 5; // Присваивание переменным a и b значения 13 и 5 соответственно
printf("a=%d b=%d\n", a, b); // Вывод на экран значений переменных a и b с помощью функции printf
x = a / b; // Присваивание переменной x целочисленного значения от деления a на b
printf("x = a/b : c=%.4lf\n",x); // Вывод на экран значения переменной x с помощью функции printf
x = (double) a / b; // Присваивание переменной x вещественного значения от деления a на b
printf("x = (double) a/b : x=%.4lf\n",x); // Вывод на экран значения переменной x с помощью функции printf
c = (double) a / b; // Присваивание переменной c вещественного значения от деления a на b, но затем приведение этого значения к целочисленному типу
printf("c = (double) a/b : c=%d\n",c); // Вывод на экран значения переменной c с помощью функции printf
c = a % b; // Присваивание переменной c значения остатка от деления a на b
printf("c = a%%b : c=%d\n",c); // Вывод на экран значения переменной c с помощью функции printf
y = ( x = 5.7 ) / 2; // Присваивание переменной y значения от деления значения переменной x, которой присвоено значение 5.7, на 2
printf("y = (x = 5.7)/2 : x=%lf y=%lf\n", x, y); // Вывод на экран значений переменных x и y с помощью функции printf
y = (int)x / 2; // Присваивание переменной y целочисленного значения от деления значения переменной x на 2
printf("y = (int)x/2 : y=%f\n\n", y); // Вывод на экран значения переменной y с помощью функции printf, символ \n используется для переноса на новую строку.

 
 y = ( b + 1 ) / b++; 
// Значение выражения в скобках сначала вычисляется, затем к переменной b добавляется 1.
// После этого производится операция деления на b, которая даст результат,
// равный целой части от деления (b+1) на b.
// Результат присваивается переменной y.
 printf("y += (b+1)/b++ : b=%d y=%.3lf y=%.0lf\n", b, y, y);
// Вывод на экран значений переменных b и y, а также значения y,
// которое получилось после выполнения операции присваивания.
// Округление значения y до целого происходит из-за спецификатора формата %.0lf.
 b = 3 * ( y -= .5 ) + 1;
// Значение переменной y уменьшается на 0.5, затем умножается на 3,
// и к результату прибавляется 1. Результат присваивается переменной b.
 printf("b = 3*(y-=.5)+1 : b=%d y=%.1lf\n", b, y);
// Вывод на экран значений переменных b и y после выполнения операции присваивания.
 z = ( c /= 2 ) + 1 / 4 * z - y++ + ++b / 3.;
// Значение переменной c делится на 2, результат прибавляется к переменной z.
// Затем происходит следующее:
// - К переменной b добавляется 1
// - К полученному результату добавляется 1/4 от текущего значения переменной z
// - К полученному результату вычитается значение переменной y, после чего к этой переменной прибавляется 1
// - Полученный результат делится на 3, после чего добавляется к переменной z.
// Все вычисления происходят с учетом текущих значений переменных.
// Результат каждой операции присваивается переменной z.
 printf("z = (c/=2) + 1/4*z - y++ + ++b/3. :\n\
 a=%d b=%d c=%d x=%lf y=%lf z=%lf\n", a, b, c, x, y, z);
// Вывод на экран значений переменных a, b, c, x, y и z после выполнения всех операций.
 return 0;
}
